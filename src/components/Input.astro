---
const {name, type, placeholder} = Astro.props;
---
<input 
  type={type}
  name={name}
  placeholder={placeholder}
  class="border-[1.5px] border-[#8989A2] rounded bg-white text-primary-100 outline-none 
      font-medium w-full p-2 md:p-3 mb-4 focus-visible:border-secondary-200"
/>